// program to print ASCII value of a character

#include <stdio.h>

int main() {
  char alphabet;
  printf("Enter the character you would like to print the ASCII value of:");
  scanf("%c", &alphabet);

  printf("%d\n", alphabet);
}
